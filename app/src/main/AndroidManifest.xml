<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="detai.android.thitoec">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="detai.android.thitoec.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="detai.android.thitoec.TracNghiemToeic">

        </activity>
        <activity android:name="detai.android.thitoec.RuleToeicActivity" />
        <activity android:name="detai.android.thitoec.ScoreToeicActivity" />
        <activity
            android:name="detai.android.thitoec.ConfirmActivity"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog" />
        <activity android:name="detai.android.thitoec.ThiTracNghiemActivity" />
    </application>

</manifest>